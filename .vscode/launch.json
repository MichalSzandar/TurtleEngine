{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run SFML Game",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bin/SFML_Game",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb"
        }
    ]
}
